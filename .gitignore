# ── 构建产物 ──
bin/
obj/
dist/
*.user
*.suo
.vs/
.idea/

# ── 运行时产生的本地文件 ──
visitapi-editor.txt      # 每台机器记住的工作区路径，因人而异
*.bak                    # 编辑器覆盖 .dlg 前留的备份

# ── 内部工作记录 ──
# Memory.MD 是开发过程的流水账，含本机绝对路径与大量内部推理，不适合公开。
# 想公开的话删掉这一行即可。
Memory.MD

# ── 界面原型留档 ──
# mockups/ 里的 *.built.html 每个约 5MB，内嵌了游戏截图与角色美术；
# bg/ 同理。这些是第三方美术资源，不随源码发布。
mockups/
