<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AssemblyName>VisitAPI.Editor</AssemblyName>
    <RootNamespace>VisitAPI.Server</RootNamespace>
    <!-- 0.6.0：新增 BOT 外观（SEC.02）和商人货架（SEC.05）两个模块 -->
    <!-- 0.6.1：这两个模块改用独立的「内容库」目录，会自动探测
         （恰好 1 个直接用 / 2 个以上弹窗挑 / 一个都没有让人自己填路径）
         0.7.0：BOT 外观界面重做 —— 顶栏选 BOT、四张部位卡、图片网格选外观、预览图
         0.8.0：BOT 服装改版 —— 左装配台/右衣柜、每个部位一个图标（原版那一池归成「默认数据」）、
                衣柜只列作者自己做的、替换写权重 1000000、恢复默认从 SPT 原版读回、
                上装与手对不上当场标红
         0.8.1：语音也纳入进来 —— 它在 bot json 里的槽和别的部位同形（id → 权重），
                所以一视同仁当第五个部位处理（左边五组、右边五个页签），不再是只读的一行
         0.8.2：保存只送**改过的那几份**。原来一股脑全送，作者改一个 BOT 就把目录里 40 份
                全重写一遍、平白留下 40 个 .bak
         0.8.3：预览图找不到「这个部位的 id」时退回「这条记录的 suiteId」——
                一件上装的身体和手出自同一条记录，作者放一张图就够（原来要放两张一样的、
                还得各自改成不同的 24 位十六进制名，全套 69 张）
         0.8.4：预览图还能按**角色名**配 —— 一个角色在数据里是三条不相干的记录
                （上衣/下装/头），没有"角色 id"把它们串起来，名字是唯一的线索。
                匹配收得很紧（整名相等，或显示名以「文件名＋一个空格」开头），
                所以 HK416.png 不会误伤 HK416YT
         0.8.5：服装卡片每列最小 200px→250px（2560 的屏上从一行 9 张变 7 张）。
                200px 时 9 列刚好卡在临界点，窗口宽一像素少一像素就跳一列
         0.8.6：修按角色名配图的两个洞 —— 比较时**忽略空格**（作者的文件叫 MG4Damage.png，
                显示名却是「MG4 Damaged JK」，原来一张都配不上）；**多个都对得上时取最长的**
                （原来战损那套会套上普通那套的图）。每张图挂 title 写明它来自哪个文件
         0.8.7：BOT 立绘也能按角色名配 —— bot 的标识是类型名（bosskilla），作者脑子里是
                角色名（Killa），所以放宽成"文件名是类型名里的一段"，同一角色的
                bosskilla / bosskillaagro 共用一张
         0.8.8：**去掉 BOT 配图**（官方没多少 BOT 立绘可放，逼作者凑图不如让这块一直显示
                真正有用的东西）。立绘窗口保留，一律画装配示意图。配图说明从「选 BOT」弹窗
                挪到衣柜底下，且只在还有卡片缺图时才出现
         0.8.9：全站滚动条改成 AIC 样式（直角 · 石墨灰 · 斜纹滑块 · 拖动时才亮黄）。
                浏览器默认那根圆头亮灰的在这套工业面板里太出戏
         0.9.0：**商人货架改版** —— 左「货架预览」／右「参数」。预览 1:1 照搬塔科夫原生的
                商品展览（页签条/商人条/工具条/格子墙），瓦片按物品**真实占格**铺
                （items.json 的 Width/Height ＋ 武器的 ExtraSize*），左上价格·右下数量·左下等级；
                格子里画 handbook 的**分类图标**（新路由 /hbimg）。右边调价格/货币/忠诚等级/
                库存/限购/盒内数量，改完左边立刻变。保存只送改过的那几份
         0.9.1：货架页四改 —— ①商人头像画**真图**（新路由 /avimg：先找作者自己那张，
                base.json 的 avatar 指的图一般在模组 res\ 里；再退回 SPT 自带的 12 张；
                都没有才退回剪影）②「任务」页签能点，跳到任务编辑页 ③点墙上的**空格子**
                直接开物品选择器加商品 ④工作区改成和使用说明页同款背景（工程图纸壁纸透上来）。
                顺带修掉一个真 bug：place() 拿 e.currentTarget 定位，而它在派发结束后就变回
                null —— 第一次开物品选择器要先拉物品表（异步），回来时定位当场抛，
                弹窗画得出来但接线整段没跑到，表现成"能开、点不动"
         0.9.2：① 货架页的物品选择器**换成任务编辑页那个窗口**（#ipick）：可拖动、左边分类树、
                右边带中英名和 handbook 参考价。原来那套 .aspick 整段删掉，AITEMS 并进 QITEMS，
                新商品的起手价改用 handbook 参考价（原来一律写死 1000₽）
                ② **全站自定义鼠标指针**（新文件 cursor.css）：12 种形状 × 明暗两套，
                硬边直角、骨白本体 + 近黑描边（浅色主题反过来），黄只用在右下角那块小挂牌；
                75 处 cursor 声明全部改走 CSS 变量（cur 那一族）；拖动时给 html 打 data-drag 锁住全屏形状；
                设置页 APPEARANCE 卡新增开关（关掉退回系统指针，照顾指针放大/高对比度）
                ③ 顺手补的守门人：test-i18n 现在也扫 assort/bot/modroot.js 的写死键、
                查 a_*/b_*/mr_* 死文案（一开就清掉 19 个前几轮改版的遗留）、
                查有没有漏网的裸 cursor 关键字；test-modui 加了断言项数硬下界
         0.9.3：货架页图例说明从**墙底下**挪到**右边参数栏末尾**（钉在墙下时横着占满预览区，
                等于从下面切掉一条格子）；参数栏改回实底不透工程图纸壁纸（一大段小字压在网格上很吵），
                壁纸只留在左边货架那半边
         0.9.4：货架墙**照游戏改成固定 12 排高**（原来是"一排 10 格 + 高度随窗口"，
                非全屏时只有 11 排）。格宽 = 可用高度 ÷ 12，列数按宽度铺满，
                排满往下滚；「一排几个」的密度开关（I/II/III）随之去掉。
                美术补了一批终末地零件：行列标尺（超出 12 排的行号标橙）、墙的四角刻度、
                6 秒一轮的青色扫描线、选中瓦片的四角括号、空格子跟随鼠标的虚框加号。
                瓦片上的价格/数量字号跟着格宽缩，免得窗口一矮就被切掉半截
         0.9.5：改回**一排 10 格**。0.9.4 把列数做成"按宽度铺满"，在 Tech Leader 的屏上
                铺成了一排 23 格。现在 10 格 x 12 排两个都定死，格宽 = 两者里小的那个算出来的
                结果（只按高度算窄窗口会横向溢出，只按宽度算矮窗口会竖着溢出）
         0.9.6：「还原备份」页上线（SEC.06）——四张卡对应 服装/对话/任务/货架，
                还原＝把 .bak 和现役文件**对调**（点错再点一次就换回去）；现役已删的还原＝复活。
                新接口 GET /api/backup + POST /api/backup/restore（沿用兄弟接口那套文件名牢笼）。
                任务目标高级参数补上三个高频数组：命中部位（原版只用过 7 个部位，勾选组）、
                savageRole（选项从原版任务提取，新接口 /api/quests/roles——任务里是 bossKilla
                这种大小写，bots\types 目录名全小写对不上，照目录列会教人写错）、
                武器型号（复用 #ipick 逐件累积）。敌人装备/改装件仍不做（双层列表、原版各 2 条）；
                anim: 拍板不做入口（插件未实装，见 Memory）
         0.9.7：修「预览分隔条拖完、切页回来弹回默认宽」——拖到的宽度原来只写行内样式，
                切页整页重建 DOM 就丢；现在记进 splitW 变量、回到对话页时按当前窗口钳一遍重放
                （probe-roles 钉了「切页回来宽度不变」）。还原备份页版式同批修正：
                别再用设置页窄栏 .setwrap，四栏铺满全宽、长列表卡内滚
         0.9.8：偏好搬家——语言/主题/指针开关/新手引导记号/源码窗位置从 localStorage 迁到
                visitapi-editor.txt 的 pref.* 行。根因：端口每次启动随机挑，而浏览器按
                「地址+端口」隔离存储，端口一换上次存的全部作废（症状=引导每次都弹、
                语言主题每次悄悄回默认）。页面下发时整包注进 window.PREFS（脚本第一行就
                拿到、不闪错主题），改动走 POST /api/pref；引导记号双写 localStorage
                兼容测试探针
         0.9.9：清小口子四件——① 撞名守门扩容：五份样式两两互查 + 六份脚本的 JS 顶层
                全局名查重（cursor.css 故意不查：它的职责就是跨文件摸类改指针）；
                ② test-dlglink 期望值改从快照现算（独立粗正则和 C# 解析器互证），
                换快照不用再对表；③ qe_broken_file 文案去掉"任务"二字（这个码 BOT
                校验器共用）；④ dist 目录清退，发布输出只剩 publish
         0.10.0：「工程」上线——三个根（.dlg 工作区/任务库/内容库）打包成 .vaproj（键=值），
                设置页 PROJECT 卡：存成工程 / 最近五条一键切 / 手填路径打开。
                打开走"先全验后应用"（三条路径有一条不在就整个不动——SetRoot 要求已存在而
                另两个会自动建目录，三连调用会切一半；换机器场景自动建只会建空树）。
                切换前拦四处脏（dirty/qdirty/bdirty/adirty），切换后清光旧根缓存
                （QD/QROOTS/QL/BD/AD/ATPL/ABENV + 对话页退回起手模板 + 重拉素材和横幅）。
                最近列表存 pref.recent.0..4（txt 陌生顶层键会被旧版抹掉，pref 才能往返）。
                顺手修：单换任务库时漏清挂接缓存 QL。导出模组 zip 拍板不做
         1.0.0：**首个正式版**。七个模块（介绍 / BOT 服装 / 对话 / 任务 / 商人售卖 / 还原备份 /
                使用说明）齐了，986 项回归全绿，对外文档（两份 README + 两份架构说明）补齐并核对过。
                发布前把全部源码通读了一遍，修掉六个**测试全绿也照样存在**的洞：
                ① setstatus 写了个拼错的状态名 → 解析器给 -1 → DialogWriter 拿它索引 StatusNames
                   直接 IndexOutOfRange，保存/查看.dlg/挂任务当场 500。同文件的门控写手
                   Statuses() 早有这道守卫，Directives() 那行漏了
                ② 变不成模型的文件头行会被**静默删掉**：tab: always（4.0.13 写法）和写坏的 when:
                   占了头行位子却没值，回写时那条 HeadLine 返回 null → 整行从作者文件里消失，
                   不报错不警告。C# 的 when 本来有 KeepRaw、tab 没有，JS 两个都没有 → 两边补齐 + 各报一条警告
                ③ 源码窗的「下载」按钮是死的：调的 tryDownload()/fileName() 在"原型下载路径全部删除"
                   那轮就没了，按钮和接线漏删，点下去只抛 ReferenceError。连同 src_dl/saved_dl 两个键摘掉
                ④ /api/quests/links 的 Nodes() 没 try/catch，一份坏 .dlg 就能让整页挂接 500 ——
                   而兄弟调用 DlgLinks.Scan 专门为坏文件做了 broken 机制，正好被绕过
                ⑤ CustomTraders 把 `trader: "名字"`（4.0.13 不写 id 的写法）里带引号的名字当商人 id 列出来；
                   共享库 DialogHeaderParser 早有防呆，这边漏了
                ⑥ ModLooks 的类注释错挂在 ModRoot 上
                发布侧：build.ps1 还在往 0.9.9 已清退的 dist\ 出包、且不先删目录（第 8 节记着的坑），
                改成 publish\ + 先删 + 只留一个 exe；tests/README 的脚本表 25 个补齐 13 个；
                两份 README 和两份 ARCHITECTURE 补上 BOT/货架/备份/工程四个模块，
                并改掉架构文档里"将来支持编辑触发器时…"这句（0.5.0 就做了，已经是错的）
         1.0.1：一批小修（对话↔任务的两个洞、触发点补齐、旁白顺序、换任务库、文档跟到最新）。
                ① **挂接不实时**：/api/quests/link 是服务端当场重写 .dlg 文件，而对话页的 doc
                   是打开那一刻读进来的，两边从来不同步 —— 任务页挂上了、对话编辑器里看不见，
                   得 F5 再打开一次；更糟的是反过来，拿旧模型在对话页按一下保存，刚写进去的
                   那条挂接又被盖回去。现在 linkApply 完就 dlgSync（同一个文件当场重载，
                   改脏了 loadDoc 自己会先问一句），反向存盘完 linksRescan 重扫挂接表；
                   挂接顺带整包重扫，两步选择器里"这个选项已经挂了什么"也跟着更新
                ② **任务 ID 只能看不能挑，而且看的是句占位话**：对话页点「完成/上交」只往模型里
                   塞一句「（选择任务）」，作者既看不见 id 也改不了，存盘就把那四个字当任务 id
                   写进 .dlg，游戏里查不到、门控自动补上以后那个选项干脆不出现。
                   现在 optMenu 挑完不关窗、原地重开，任务 ID 那一行点开就是任务库列表
                   （本商人的排前面）＋手填格；没挑到任务就不落笔。同一个占位坑在门控 if: 和
                   文件头 tab: 上各有一份，一起改掉。芯片挂 title 显示"任务名 · 完整 id"
                ③ 顺手：菜单在 render() 之后原地重开会飘到屏幕左上角（事件里那个按钮已经是
                   游离节点，量出来的位置全是 0），optMenu / tabMenu 改成按下标现取锚点
                ④ **触发器的 auto / accept 被编辑器抹掉**（跟着插件那两个新参数一起补的）：
                   trigParse/trigLine 只认 dist/radius/hit/node/if/free，动一下任何字段
                   重拼那一行时这两样整段消失，不报错不警告 —— 进游戏才发现"走过去不自动接任务了"。
                   现在两边都认，表单里给了 auto 开关和「顺带接哪个任务」（复用挑任务窗）。
                   顺带补上 C# DialogWriter.Trigger 漏掉的 radius（只在 Raw 为空时才走到，但两边该长一样）
                ⑤ **触发点进节点的 ⋮**：「节点的身份」原来只有 入口/初见/分层入口，
                   偏偏少了第四种"玩家走过来打开这一屏"。现在菜单里能直接加（落点自动填本节点），
                   并列出指着这一屏的那几条；**没写落点的也列**（它们走 start:，落的就是这一屏）
                ⑥ **旁白可以写在台词后面**（动到共享库＋插件＋编辑器三层，插件同为 1.0.1）。
                   根因：模型把一屏拆成 narr[] ＋ npc 两个字段，**行序在解析那一刻就丢了** ——
                   写在台词后面的 `>` 行读进来会排到台词前面，存一次盘就永久换位置。
                   加 DialogNode.NpcAt（台词排在第几条旁白之后，-1＝老规矩"在最后"，老文件零影响）：
                   解析记下来、DialogWriter 按它切两段吐、插件 DialogTemplateBuilder 按它串播放链
                   （背景/BGM 挂到真正的第一拍，OptionMap.Entry 的入口判断跟着改）。
                   界面上拍条按真实顺序画、台词那拍带下划线，右边 ◀ ▶ 挪一格、✕ 删一段
                   （台词那拍删不掉——它是 npc 字段本身，但可以挪到最前面）
                ⑦ **任务页头部常驻「换任务库」**。原来切库的入口只在"没指过 / 空库"时才够得着，
                   库里一有任务就再也换不了（"我的任务怎么不见了"十有八九就是指错了库，却没地方改），
                   只能绕道设置页的工程。**这颗按钮本身是小事，它逼出来的两样才是**：
                   脏检查——老入口只在空态可达所以从来不用拦，做成随时能点之后不拦就是一按按钮
                   把没保存的任务全扔了（QD 整个换掉，没有回头路），现在照 qReload 那道闸走；
                   回头路——rootPane() 原来只进不出（它只在没库时出现，本来也没地方可回），
                   用 qRootBack 分辨"主动来换"和"压根没库"，只有前者给「不换了」，
                   后者给取消等于给死胡同
                ⑧ 新手引导 19 步 → 28 步（补：商人台词、选项的 ⋮、跳转、拍条顺序、旧版兑存、
                   折叠预览；任务页补：对话挂接、校验那一栏、换任务库），说明 18 张 → 20 张
                   （新增「三个文件夹」和「还原备份」两张，节点/触发点两张跟着改）。
                   ⚠️ 写这一批时踩到：**中文文案里混进 ASCII 双引号会把整个脚本语法搞崩，
                   而 i18n 守门人是正则扫的、抓不到** —— 中文引号一律用 「」

         1.0.2：① **选项的好感增减终于能编了**（optMenu 新增「好感增减」一段）。
                   `standing:` 从解析、写手到插件本来就是通的，缺的只有入口 —— 芯片画得出来、
                   菜单里编不了，作者只能回文本里手改。现在：预设六档（不变／−0.05／−0.01／
                   ＋0.01／＋0.05／＋0.1）＋手填格（只认回车；空／0／不是数一律当「不变」，
                   因为插件 `delta==0` 根本不注册）＋「加给谁」（默认本对话的商人，
                   也能从商人表挑别人 → `standing: <商人id>=+0.05`）。
                   改值不碰商人 id；值清掉时商人 id 一起清掉（不然写手会吐出一条
                   `standing: 商人=0` 的空指令，插件不认、文件里却多出一行没人看得懂的东西）。
                   选项芯片在指了别的商人时把商人名带出来。
                   ⚠️ `setstatus:` 仍然只读不可编 —— 同一个洞的另一半，还没做。
                ② **存盘报 500 时会说人话了**。`/api/dlg` 原来是裸奔的：写手抛一次、目录被删了、
                   文件只读、被别的程序占着，界面上只剩一个光秃秃的「500」——
                   日志提供者在 Program 里被清掉，控制台连一行都没有，作者和我们都无从查起。
                   现在 try/catch：控制台打全栈、回一句中英文原因（只读／目录没了／被占用）。
                   顺带修掉两条真能 500 的路：**目标目录不在了**（打开时还在、存的时候没了，
                   现在自动补建，不让作者丢掉这一次修改）、**文件头别名对不上**
                   （`quest a = …` 的 a 不在别名表里 → KeyNotFound，现在当这一行不存在）

         1.0.3：货架页两条，都是**真把别人的存档写坏了**才发现的。
                ① **`document.querySelectorAll` 把 `<html>` 一起选中了。**
                   `wireAs()` 用 document 级选择器接 `[data-cur]`，而全站的鼠标指针开关
                   （index.html 的 `setCur()`）往 `<html>` 上盖了一个 `data-cur="on"`，默认就开着。
                   于是点击处理器被装到了**冒泡的终点**上：这一页上任何一次点击都顺带跑一遍
                   `aCur("on", undefined)`，往 barter_scheme 里写一条 `"on": [[{"count":0}]]`
                   —— 键不是 24 位十六进制、还缺 `_tpl`（`JSON.stringify` 把 undefined 直接丢掉）。
                   SPT 把这张表的键反序列化成 MongoId，读到 "on" 当场抛，**服务端启动即停**；
                   而编辑器界面上**完全看不出来**（所有读路径都是拿商品 id 去查价格表，
                   没有一处遍历这张表的键）。2026-08-22 SORA 的货架就是这么被写坏的。
                   三道一起上：接线收进本页作用域（`qAs`）＋属性改名 `data-acur` 不再和别人撞
                   ＋ `aPrice`/`aCur` 先认人再落笔（id 必须是货架上真有的商品、货币必须是那三种）。
                   服务端还加了第四道 `AssortValidator.FatalId`：**非法 id 一律不许落盘**，
                   保存直接 400（别的校验都是"先存下去再提示"，这一条不行 —— 存下去就开不了服）。
                   顺带把"价格条目缺 `_tpl` / 是空的"做成 err（`as_price_bad`）：服务端能启动，
                   但玩家在货架上点这件东西时客户端当场抛。原版 420 件货架实测零误报。
                ② **价格改不了、只能一下一下点上下箭头。** 四个数字格（价格/剩余/限购/盒内数量）
                   接的是 `oninput`，第一个字符敲下去就 `render()` 重画整块面板，
                   输入框连人带光标一起被换掉，第二个字符没人接了；能生效的只剩下鼠标点箭头，
                   而 `step` 是默认的 1（从 1000 点到 12000 要点一万一千下）。
                   四个格子一律改 `onchange`（回车/失焦才落笔，点箭头照样即时生效）——
                   任务页 quest.js 早就是这么写的，那儿的注释写着同一条理由。
                   价格顺带钳成非负整数（`min="0"` 只是浏览器的皮，挡不住 JS 写小数/负数）。

         1.0.4：货架页的"数据不丢"补完 + 1.0.3 自己带出来的两个洞。
                **1.0.3 那两条价格的钳制是错的，这一版撤回**：① 四舍五入抹小数 ——
                原版一口价里 **649 条是小数**（和平使者的美元价、滑雪佬的欧元价，
                像 261.59 / 15192.74），点一下上下箭头就被抹平；现在只钳负数。
                ② `aSimple` 只数层数不看"拿什么付" —— `[[{count:1,_tpl:<一件物品>}]]`
                和一口价形状完全一样，**原版 2854 条里有 400 条正是这种单件以物换物**，
                照样拿到货币按钮，点一下"换一捆电线"就变成"1 卢布"；
                现在改叫 `aFlat`，要求那一样必须是认识的三种货币之一。
                其余（都对着原版真数据核过的）：
                · 改价格/换货币**就地改一个字段**，不再重建整条（原版 168 条带
                  level/side/onlyFunctional，重建即抹）；空数组 `[]` 也算"还没定价"，
                  否则它既编不了又永远报 as_price_bad，成死结。
                · 删商品收**整条子孙链**（原版 234 件商品的子件不止一层），
                  两张表按整条链清；新增 `as_orphan_item`（warn）盯住历史遗留的孤儿子件。
                · 库存档位：点已经选中的那一档＝一个字段都不造；切档保留作者填的数，
                  **只有 9999999 那种用不完的哨兵值**切回有限时换成 1
                  （原版 2822 件无限商品里 2657 件写的就是它）。
                · 限购：不清零作者的"已买几个"，也不为了删两个键凭空造一个 `upd`。
                · 合计只算货币，以物换物单独报个数（原版耶格尔那面墙原来会渲染出 69 个 ₽ 碎片）。
                · 实时预览改成**只换那一块瓦片**：整墙重画在四百件的货架上每敲一个字符重建
                  四百个节点，而且正被按住的瓦片一旦被换掉，抬起时 click 不成立
                  —— 表现成"打完字点别的商品第一下没反应"。值没变时干脆不重画。
                · `qAs` 取不到本页根节点时**返回空**而不是退回 document（失败要关闭，不能悄悄
                  把刚修的洞打开）；`aLoyal` 补上和 aPrice/aCur 一样的"先认人"；
                  `aBarter` 每层都当脏数据收（它在接线之前跑，抛一次整页点不动）。
                · 存盘报错按**字段**判不按子串判（一份叫 stale.json 的货架会把 bad_id 认成冲突，
                  弹框→确认→再 400，死循环）；`FatalId` 改成走根对象和它的每个对象子节点，
                  认不出来的文件形状**失败关闭**；缺字段时回字段名，不再把中文塞进 id。
                测试：这一版的每一条修复都做了"撤回就红"的负向验证（分两轮跑，10 条断言逐一对上），
                并把三处假绿改掉：断言挂 `.aspar` 恒真（那块里常驻两句带 × 的文案）、
                打字用同一个 JS 对象（元素被换掉了它自己那份 oninput 照样写模型）、
                比子串（`includes("123")` 会命中 "123 456"）。probe 末尾**真按一次保存**，
                把"编辑器手里这份模型"和"服务端愿不愿意收"接上；`$FLOOR` 跟着抬到 195。          1.0.5：① **分支记号 `set:` / `ifvar:`**（共享库＋插件 1.2.0 同步）：选项 ⋮ 里新增「分支记号」
                两格（记一笔 / 只在记号等于…时显示），芯片、说明页、兼容检查一起认。
                ② 触发器表单：`enter N` 型（进图计时、没坐标）以前 TRIG_RE 认不出、一动表单就被
                重拼成坐标型——现在认了；「任务门控」那格从手打文本改成任务选择器＋状态芯片。
         1.0.6：任务属性面新增「VisitAPI 扩展」两个开关（插件/服务端 Rework DEV_NOTES #67 同步）：
                「目标二选一」→ `visitapi.anyOf`、「达成即解锁商人」→ `visitapi.unlockTraderOnReady`。
                住在 quest.visitapi 里，SPT 原生不认、只有 VisitAPI 认；校验：开了二选一但目标不到两个 → 警告。
         1.0.7：`accept:` / `complete:` 一次能带多个任务（空格隔开：`accept: 任务A 任务B`），
                共享库＋插件同步。模型 `AcceptIds/CompleteIds` 列表，`AcceptId/CompleteId` 保留为"第一个"快捷口；
                选项 ⋮ 菜单任务行一行一个＋「再挂一个」/「摘掉这个」；任务页挂接改成加进列表/只摘自己那条。
         1.0.8：任务属性面加**章节模块**（插件/服务端 Rework DEV_NOTES #70/#71 同步）：「VisitAPI 扩展」下多三个开关
                （章节 / 可接时自动接下 / 只能通过对话接交）+ 章节图标 URL（`data-vf`，清空即删键）+「剧情日记」三格
                （`data-note`：正文存 locale、键是日记 id，第一次输入才 NEWID()）+「相关物品」（复用 #ipick，⋮ 摘掉）；
                校验新增 chapter_no_subs / chapter_not_secret / bad_note_id / note_no_text / bad_item。不另开页签。
         1.0.9：「VisitAPI 扩展」再加一个开关「达成即自动交」（visitapi.autoFinish，插件侧 Rework DEV_NOTES #74）：
                章节子任务从此不进支线/商人任务列表（照 1.1），接交只能靠对话或自动链，这个开关就是自动链的另一半。
         1.1.0：新模块**「章节编辑」**（SEC.04，排在任务编辑前面；chapter.js + chapter.css），对齐 VisitAPI 1.2.0 的章节系统。
                和任务编辑同一套骨架，卡片照 1.1 剧情页的版式、就地可编：左图标列（所有章节，＋新章）· 横幅（选图、章节名/描述直接改、
                状态挂牌）· 主要目标（按子任务分组：开关芯片、＋目标、⋮、主/可选一键翻）· 可选目标 · 日记（章节+各子任务）· 相关物品 ·
                子任务顺序（上移/下移/换/打开/摘、挑现有或新建、串成链）；顶栏三态模拟只改样子不改数据。右画布「章节册」+ 校验面板。
                数据和任务页共用一份 QD（章节就是任务 JSON），quest.js 的增删菜单按 qcur 找任务，章节页动谁先把 qcur 指到谁。
                终章标记 isFinisher 顺序一动就重算、目标行文案「完成〈子任务〉」中英各落一句；图标/横幅复用任务图选图器（gpField）。
                校验新增 chapter_no_finisher / chapter_no_icon / chapter_no_banner / chapter_sub_is_chapter，
                以及子任务的 sub_no_entry / sub_no_exit（既没自动开关、.dlg 里也没 accept:/complete: 它 → 死任务；
                校验器多收 .dlg 的 accept/complete 名单，没工作区就跳过这两条）。任务页属性面留一行「属于章节 → 去章节页」。
         1.1.1：章节模块三修（Tech Leader 实测提的）。
                ① **新建子任务变成"章节挂了自己"**（真 bug）：`createQuest` 结尾会 `render()`，而章节页重画时
                   `wireCard` 把 `qcur` 拉回当前章节 —— `newSub` 在那之后读 `qcur` 当"刚建的那条"，拿到的是章节自己。
                   改成 `createQuest` **返回新 id**，`newQuest(btn,after)` 把 id 传给回调，新章/新子任务都用返回值。
                   `addSub` 另加防呆：挂自己或挂另一个章节直接拒绝并提示。
                ② **原生通知没关**：`makeChapter` / `addSub` 补上 `canShowNotificationsInGame=false`
                   （剧情任务的横幅由 VisitAPI 出，原生要闭嘴 —— 原先只有「新建子任务」那一条路记得关）。
                   校验新增 `story_native_notify` 警告，手写 json 的人也拦得住。
                ③ **章节页新手引导**（`TOUR.chapter` 七步）：建章 → 起名配图 → 挂子任务 → 加目标 →
                   开自动接/自动交 ⚠️ → 三态是预览不是设置 → 保存。`tourMaybe` 里写死的 dlg/quest 映射
                   改成 `page===kind`。顺带修了**所有引导**共有的显示 bug：`.tourtip b{display:block}`
                   本是给标题的，20 条现成文案里句中的 `<b>` 强调因此被切成一行行黄色块 —— 加 `.tourtip span b{display:inline}`。
         1.1.2：剧情"失败还能继续"（Tech Leader 拿 1.1 正式版截图定的口径：「塔科夫之旅」里有一条
                「(已失败)…」红叉目标，章节右上角照样是「完成」）。配套插件 1.2.0。
                ① 触发点新增 **finish / fail** 两个参数（可与 accept 同时写）：走到某处/进图 N 秒后
                   把某条任务判完成或判失败，不弹对话。共享库解析+回写、编辑器面板两栏（复用挑任务窗口）。
                ② 章节页子任务 ⋮ 加「**失败也算过**」：把章节那条 Quest 条件的 status 从 [4] 换成 [4,5,6]，
                   否则剧情里被作废的子任务会把整章永远卡在交不掉。开着的子任务行上挂芯片。
                ③ 测试脚本的判失败改成 `-cmatch '^(FAIL|EXCEPTION|WINDOW-ERROR|REJECT)'` —— 原来是
                   `-match 'FAIL|EXCEPTION'`，**PowerShell 的 -match 不区分大小写**，用例名里带 fail
                   就被算成失败（新加的「finish/fail 没被重拼时抹掉」当场中招）；顺带把一直漏算的
                   WINDOW-ERROR / REJECT 收进来。11 份脚本一起改。
                ④ 侧栏/介绍页的断言不再写死"7 格""两个入口"，改成按 PAGES 现算。
                ⑤ 触发点的 accept/finish/fail 一路送到前端（DlgTrigger 三个新字段，且**有动作就列出来**，
                   原来只列带 if 的）：校验把它们并进"入口/出口"名单，否则用触发点开场的剧本会被
                   sub_no_entry 误报成「玩家接不到」；挂接页的触发点行也跟着显示"走到就接下/判完成/判作废本任务"。
                ⑥ 新校验 sub_prereq_is_chapter（死锁：子任务把所在章节写成前置，两边互相等）。
                ⑦ 存盘成功后提示一次「已保存 · 记得重启 SPT 服务端」——SPT 只在启动时读一次 db/quests。
                ⑧ 探针脚本先从 dlgsnap 铺一遍 .dlg 工作区（原来只铺 dbsnap，用的是作者真在编的那份剧本，一改就假红）。
         1.1.3：① 章节页右栏从「章节册」改成**上下两块流程图**（上＝这一章的对话节点图，复用对话页的
                   drawGraph()、无关节点压暗；下＝本章子任务链，按章节内部的前置深度自己排），中间可拖。
                   ⚠️ 容器 id 必须原样叫 graph/gvp/edges/gnodes（借的是对话页那套全局），但**节点的点击要重接**——
                   原版 onclick 走 refresh()，那要用到 #bglayer 这些只有对话页才有的 DOM。
                ② 和插件做了一次**功能对账**，补上三个"格式/引擎支持而 UI 不支持"的洞：
                   · 目标的**显示条件**（visibilityConditions/CompleteCondition：前一条目标完成了这一条才冒出来。
                     原版 558 个任务里 197 处这么用，插件剧情页早就认 CheckVisibilityStatus，就是没入口）
                   · 选项的 **setstatus:** 编辑入口（老洞：芯片一直画得出来、菜单里编不了）
                   · 任务页目标行的**主要/可选**（isNecessary）——章节页早就有，普通任务这边一直缺
         1.1.4：**.dlg 里的任务 id 校验**。写错的 id（占位符、拼错的别名、手抖）在游戏里查不到任何任务，
                而自动门控会顺手把那个选项藏起来 —— 现象是"这个选项莫名其妙不出现"，不看文件根本查不出来。
                检查放在**别名解析那一处收口**（C# 的 DialogParser.A / 前端 badQids）：accept/complete/handover/
                setstatus、if/ifnot、tab、触发点的 accept/finish/fail 全都从那儿过，一处就管住全部。
                三个地方报：对话页顶栏状态条（附完整清单在 title 里）、选项行的芯片标红、任务页的校验面板
                （码 dlg_bad_qid）。⚠️ 触发点存的是原文没过别名表，判之前得先自己解一次，否则
                `accept: sora2` 这种合法写法会被冤枉。
                ⚠️ 校验码一律住在校验器里（dlg_bad_qid 一开始发在 QuestApi 里，test-i18n 的"死文案"守门人
                当场报错——它只扫那三个 Validator，破例一次就会开始误报）。
         1.1.5：跟上插件 1.2.0 的 **ifitems**（背包里有东西可交才显示这个选项）：共享库的模型/解析/回写、
                JSON 桥、选项 ⋮ 菜单里和 once/always 并排的开关、选项行的芯片、说明页的指令表。
                无参写法 `ifitems` = 用同一选项 handover: 的那条任务；`ifitems: 任务` 可以指名道姓。
         1.1.6：**对账剩下的三个洞补完**（至此 `.dlg` 格式支持的每一样，编辑器都编得了）：
                · `handover:` 的**物品说明**（`handover: 任务 建筑材料`）—— 上交动作下面多一个输入框，回车生效。
                  它只是给玩家看的一句提示，**不决定真正交哪些物品**（那个由任务目标决定），说明里写清楚了。
                · 门控的**多状态**（`if: q=Started/AvailableForFinish`）—— 新增 segN（多选版的 seg2），
                  点一下加/减一个状态，至少留一个（全点掉应该走「不设」）。
                · **第二条门控** —— 原来解析器把它搁在 gate2 里原样带回（不丢数据）但编不了。
                  ⚠️ 格式只放得下**一条 if: + 一条 ifnot:**（模型是两个单值字段），所以第二条的种类
                  自动取第一条的反面，不给它单独的种类开关；撤掉第一条时第二条顶上来，别留孤儿。
                ⚠️ 测试教训：探针里 `.kebab` 画的是**当前节点**那几行，拿别的节点的 opts 下标去点必然错位；
                第一版两个分支双双走进"跳过"，13 条断言一条没跑还全绿。现在先按"哪一屏有这种选项"切过去，
                并且加一条「找得到…来测」把跳过本身也断言掉。
         1.1.7：**修外部作者报的"打开一个菜单以后，别的输入框点不进去"**——真复现了，
                但不是"商人货架"那一页，是**对话页的文件头菜单**：它一打开就把焦点放在 scene 上，
                而两个输入框挂了 `onblur=put`，`put()` 末尾是 `render()`。
                于是你去点台词框 → scene 先失焦 → 整页重画 → 你刚点中的那个框已经不存在了。
                改法：失焦里不许 render()。没改动就直接返回；改了也只把顶栏那两格的字换掉。
                双击节点改名同一个坑（blur 无条件 refresh()），名字没改就把标题原样放回去。
                探针新增一组"焦点不该被抢"：把每个菜单开一遍，再派发 mousedown+focus，隔帧看焦点还在不在。
         1.1.8：介绍页顶栏的「模块 n/m」报错了数——**章节模块做完了却漏加进 AB_READY**，
                模块表里一直给它挂着「未实现」牌。同时分母含介绍页、分子不含，本页永远被少数一个。
                改：AB_READY 加 chapter（它的真实含义是给「去」按钮的模块，介绍页不在里面是因为你就在上面），
                顶栏改用 AB_DONE = AB_READY.length + 1。探针加三条把这个数钉死在模块表上。
         1.1.9：**窗口没最大化时介绍页/说明页滚不到底、右下角刻度括号飘在半空**。
                根因是 `.abin` / `.hpin` 上的 `min-height:0`——它等于允许 flex 把这层压到比内容还矮
                （实测 644px vs 内容 1370px），而外层 `.abwrap` 是按它算滚动范围的，于是底部够不着；
                那对角标钉在它的底边上，自然也飘到了半空。去掉 min-height:0 即可（默认 min-height:auto
                本来就不肯缩到内容以下，grow 保留着，大屏照样撑满）。
                顺带：模块表挂牌写 7/8、顶栏写 8/8，两个数打架，统一走 AB_DONE。
                ⚠️ 测试教训：介绍页的测试一直跑 2560x1400，"窗口一矮就散架"从来测不到。
                现在探针会把 .shell 临时钉成 700px 模拟没最大化的窗口再量一遍。
         1.1.10：**使用说明页补齐 + 加导读**。对完账，说明页落后了整整一代：
                 整个「章节编辑」模块一张卡都没有；「改完任务必须重启 SPT 服务端」一个字没提；
                 任务上那一整栏 VisitAPI 开关（anyOf/自动接/自动交/dialogOnly/解锁商人/日记/相关物品）全缺；
                 触发点只写了 accept，没写 finish / fail。新增三张卡 G16 / G17 / G18，两种语言各一份。
                 另加一张**不折叠的导读卡**（REF.G00）钉在最上面：一张「我想干什么 → 去哪张卡」的表，
                 别让人一进来对着 23 个关着的标题发呆。
                 ⚠️ 两个自己踩的坑：①导读卡一开始没写成 .card，黄条和"编号靠右"都是 `.card>.chead` 给的，
                 白写一套；②那句问句写成了带 font-size 的 <p>——probe-help 里"表格和正文一样大"
                 取的基准就是**第一个 <p>**，基准被顶大，整条断言就废了。现在它是独立的 .hpq。
                 新增 tests/shot-help.ps1：全屏 / 矮窗口 / 英文各拍一张，版式眼见为实。
         1.2.0：**新手引导覆盖到每一个模块**。原来只有对话 / 任务 / 章节三条，
                 BOT 外观、商人售卖、还原备份、程序介绍、使用说明这五页一步都没有，
                 render() 里也没给它们调 tourMaybe。现在八个模块一个不落，新增 33 步文案（中英各一份）。
                 引导的步多了个**第三项＝先按这个才看得见**：任务页的「VisitAPI 扩展 / 剧情日记 /
                 相关物品 / 串起来」都住在「属性」那一屏上，引导会替用户切过去再讲。
                 （每步只切一次，切完还找不到就跳过——不设这个闸，prep 点了不生效就是死循环。）
                 ⚠️ 抓到两个"步骤写了等于没写"：①那四块不切屏永远不出现；
                 ②「串起来」我挂在章节页上，可它的按钮其实在**任务页**的属性屏里。
                 新增 tests/test-tour.ps1（18 项）：八条引导逐步走一遍，验每一步都落在真元素上、
                 标题说明不是 key、高亮框不小于 8px、气泡不出屏，外加"跳过/走完都要记住"和"目标不在整步跳过"。
                 test-i18n 那条"引导指的元素找得到"改成扫 wwwroot 下**所有** .js（原来只扫 quest/chapter，
                 于是 BOT 和货架的 10 个选择器被误判成指着空气）。
-->
    <Version>1.2.0</Version>
    <!-- 构建时间写进程序集版本号（会拼成 0.1.0+b260808-0030）。
         不能靠读 exe 的文件修改时间——用户从 Release 下载后那个时间会变成下载时间，
         显示的版本就是假的，等于白做。 -->
    <SourceRevisionId>b$([System.DateTime]::UtcNow.ToString("yyMMdd-HHmm"))</SourceRevisionId>
    <!-- 分发形态：单文件 exe。framework-dependent（不自带运行时）——
         装了 SPT 的人本来就有 .NET 10 + ASP.NET Core（SPT.Server.runtimeconfig.json 写死的），
         所以不打包运行时，体积能小两个数量级。 -->
    <PublishSingleFile>true</PublishSingleFile>
    <SelfContained>false</SelfContained>
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
    <InvariantGlobalization>true</InvariantGlobalization>
    <!-- exe 自己的图标。同一个文件在 wwwroot 里还有一份，当网页的 favicon（见下面的嵌入规则） -->
    <ApplicationIcon>wwwroot\favicon.ico</ApplicationIcon>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\VisitAPI.Dlg\VisitAPI.Dlg.csproj" />
    <ProjectReference Include="..\VisitAPI.Quests\VisitAPI.Quests.csproj" />
  </ItemGroup>

  <!-- 界面整包嵌进 exe：单文件分发时 wwwroot 不会自动跟着走，嵌成资源最稳 -->
  <ItemGroup>
    <EmbeddedResource Include="wwwroot\**\*" LogicalName="ui/%(RecursiveDir)%(Filename)%(Extension)" />
    <Content Remove="wwwroot\**\*" />
  </ItemGroup>

</Project>
